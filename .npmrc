# pnpm workspace 配置
# 允许 workspace 内包以 workspace:* 协议互相引用时优先走本地链接
link-workspace-packages=true
prefer-workspace-packages=true
# RN / Expo / Taro 对 hoist 较敏感，保持 node-linker=hoisted 以兼容原生工具链
node-linker=hoisted
strict-peer-dependencies=false
auto-install-peers=true
